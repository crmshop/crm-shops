# Backend Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database e Supabase
supabase==2.0.0
psycopg[binary]>=3.1.0
sqlalchemy==2.0.23

# Autenticazione e sicurezza
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# HTTP Client - Versioni compatibili con supabase 2.0.0
# httpx è usato direttamente nel codice (gemini.py, banana_pro.py)
# Usiamo un range flessibile per permettere a pip di risolvere conflitti
httpx>=0.24.0,<0.28.0
requests==2.31.0
# NOTA: anyio e websockets sono dipendenze transitive di httpx/uvicorn/supabase
# NON specificare versioni esplicite - pip le risolverà automaticamente

# Google Generative AI (nuova libreria)
google-genai>=0.2.0
Pillow>=10.0.0  # Per gestire immagini PIL

# Validazione dati
pydantic>=2.9.0,<3.0.0  # Limita a v2 per compatibilità
pydantic-settings>=2.5.0
email-validator>=2.0.0

# Utilità
python-dateutil==2.8.2

# Testing (opzionale per sviluppo)
pytest==7.4.3
pytest-asyncio==0.21.1

